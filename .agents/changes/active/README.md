# Aktif İşler

Her kalıcı iş `.agents/changes/active/<work-id>/` altında yaşar. Oturum başlangıcında bütün klasörleri okumak yerine iş kimliği çözülür; önce `work.json` ve `status.md`, sonra yalnız mevcut aşamanın artifact'ları okunur.
